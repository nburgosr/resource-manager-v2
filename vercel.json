{
  "buildCommand": "npx prisma generate && (npx prisma migrate resolve --applied 20260627000001_add_database_backup || true) && npx prisma migrate deploy && npx tsx prisma/init-users.ts && next build",
  "framework": "nextjs"
}
